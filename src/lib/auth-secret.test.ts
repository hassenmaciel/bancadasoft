import{SignJWT,jwtVerify}from"jose";import{describe,expect,it}from"vitest";import{AuthConfigurationError,getAuthSecret}from"./auth-secret";
describe("AUTH_SECRET",()=>{it("rejects missing secret",()=>expect(()=>getAuthSecret(undefined)).toThrow(new AuthConfigurationError("AUTH_SECRET_MISSING")));it.each(["","   ","too-short"])("rejects invalid secret",value=>expect(()=>getAuthSecret(value)).toThrow("AUTH_SECRET_INVALID"));it("preserves and accepts a cryptographically adequate secret",async()=>{const value="  valid-secret-with-at-least-32-bytes!  ";const key=getAuthSecret(value);expect(new TextDecoder().decode(key)).toBe(value);const token=await new SignJWT({sub:"admin"}).setProtectedHeader({alg:"HS256"}).sign(key);await expect(jwtVerify(token,key)).resolves.toBeTruthy()})});
