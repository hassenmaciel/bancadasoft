import{describe,expect,it}from"vitest";import{productDeleteDecision,relatedEntityDeleteDecision,userDeleteDecision}from"./admin-rules";import{adminUserInputSchema}from"./admin-inputs";
describe("ciclo administrativo seguro",()=>{it("exclui produto sem histórico e arquiva com dependências",()=>{expect(productDeleteDecision(0,0)).toBe("DELETE");expect(productDeleteDecision(1,0)).toBe("ARCHIVE");expect(productDeleteDecision(0,1)).toBe("ARCHIVE")});it("protege marcas e categorias associadas",()=>{expect(relatedEntityDeleteDecision(0)).toBe("DELETE");expect(relatedEntityDeleteDecision(2)).toBe("ARCHIVE")});it("protege próprio e último ADMIN",()=>{expect(userDeleteDecision({self:true,adminCount:2,role:"ADMIN",orders:0})).toBe("BLOCK");expect(userDeleteDecision({self:false,adminCount:1,role:"ADMIN",orders:0})).toBe("BLOCK")});it("desativa usuário com histórico e exclui sem histórico",()=>{expect(userDeleteDecision({self:false,adminCount:2,role:"USER",orders:1})).toBe("ARCHIVE");expect(userDeleteDecision({self:false,adminCount:2,role:"USER",orders:0})).toBe("DELETE")});it("exige senha forte sem armazenar ou registrar senha",()=>{const base={name:"Usuário",email:"user@example.test",role:"USER"};expect(adminUserInputSchema.safeParse({...base,password:"fraca"}).success).toBe(false);expect(adminUserInputSchema.safeParse({...base,password:"Senha-Forte-123!"}).success).toBe(true)})});
