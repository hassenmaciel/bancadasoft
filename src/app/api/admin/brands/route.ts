import { NextResponse } from "next/server";import { requireAdmin } from "@/lib/auth";import { brandInputSchema } from "@/lib/admin-inputs";import { prisma } from "@/lib/prisma";import { audit } from "@/lib/audit";
export async function GET(){try{await requireAdmin()}catch{return NextResponse.json({error:"Não autorizado."},{status:403})}return NextResponse.json({data:await prisma.brand.findMany({include:{_count:{select:{products:true}}},orderBy:{name:"asc"}})})}
export async function POST(request:Request){let admin;try{admin=await requireAdmin()}catch{return NextResponse.json({error:"Não autorizado."},{status:403})}const parsed=brandInputSchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"Dados inválidos."},{status:422});try{const brand=await prisma.brand.create({data:parsed.data});await audit(admin.id,"BRAND_CREATED","Brand",brand.id,{name:brand.name,slug:brand.slug});return NextResponse.json({data:brand},{status:201})}catch{return NextResponse.json({error:"Nome ou slug já cadastrado."},{status:409})}}
