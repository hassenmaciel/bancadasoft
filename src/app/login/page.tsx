"use client";
import { FormEvent, useState } from "react";import Link from "next/link";
export default function Login(){const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})});if(!r.ok)return setError("E-mail ou senha inválidos.");location.href="/meus-pedidos";}return <main className="private-page"><form className="panel login-panel" onSubmit={submit}><Link href="/" className="brand">BANCADA<span>SOFT</span></Link><h1>Entrar</h1><label>E-mail<input name="email" type="email" required/></label><label>Senha<input name="password" type="password" minLength={8} required/></label>{error&&<p className="error">{error}</p>}<button className="primary">ENTRAR</button></form></main>}
