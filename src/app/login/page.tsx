"use client";
import Link from "next/link";import{FormEvent,useState}from"react";import styles from "./login.module.css";
export default function Login(){const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setError("");setLoading(true);const form=new FormData(event.currentTarget);try{const response=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:form.get("email"),password:form.get("password")})});if(!response.ok){setError("E-mail ou senha inválidos.");return}const requested=new URLSearchParams(window.location.search).get("next");window.location.assign(requested?.startsWith("/")?requested:"/meus-pedidos")}catch{setError("Não foi possível acessar o painel agora.")}finally{setLoading(false)}}return <main className={styles.page}><div className={styles.glow}/><section className={styles.card}><header className={styles.header}><Link href="/" className={styles.brand}>BANCADA<span>SOFT</span></Link><span className={styles.badge}>ACESSO SEGURO</span></header><div className={styles.intro}><small>ÁREA RESTRITA</small><h1>Painel Administrativo</h1><p>Gerencie a operação do BancadaSoft em um ambiente seguro.</p></div><form onSubmit={submit} className={styles.form}><label>E-mail<input name="email" type="email" autoComplete="email" placeholder="admin@bancadasoft.local" required/></label><label>Senha<input name="password" type="password" autoComplete="current-password" placeholder="Digite sua senha" minLength={8} required/></label>{error&&<p className={styles.error} role="alert">{error}</p>}<button disabled={loading} type="submit">{loading?"Entrando...":"Entrar no painel"}</button></form><footer><span>🔒</span> Sessão protegida e acesso exclusivo para usuários autorizados.</footer></section><p className={styles.back}><Link href="/">← Voltar para a loja</Link></p></main>}
