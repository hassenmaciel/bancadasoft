import { prisma } from "@/lib/prisma";import RoleButton from "./role-button";
export default async function UsersPage({searchParams}:{searchParams:Promise<{q?:string}>}){const{q}=await searchParams;const users=await prisma.user.findMany({where:q?{OR:[{name:{contains:q,mode:"insensitive"}},{email:{contains:q,mode:"insensitive"}}]}:undefined,select:{id:true,name:true,email:true,role:true,createdAt:true,_count:{select:{orders:true}}},take:100,orderBy:{createdAt:"desc"}});return <><header className="admin-heading"><div><small>ACESSOS</small><h1>Usuários</h1><p>Dados não sensíveis, função e histórico comercial.</p></div></header><form className="admin-filters"><input name="q" defaultValue={q} placeholder="Buscar nome ou e-mail"/><button className="admin-primary">Buscar</button></form><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Usuário</th><th>Role</th><th>Pedidos</th><th>Cadastro</th></tr></thead><tbody>{users.map(user=><tr key={user.id}><td>{user.name}<small className="cell-subtitle">{user.email}</small></td><td><RoleButton id={user.id} role={user.role}/></td><td>{user._count.orders}</td><td>{user.createdAt.toLocaleDateString("pt-BR")}</td></tr>)}</tbody></table></div></>}
