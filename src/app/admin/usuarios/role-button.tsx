"use client";import { useState } from "react";import { useRouter } from "next/navigation";export default function RoleButton({id,role}:{id:string;role:string}){const router=useRouter();const[busy,setBusy]=useState(false);async function change(next:string){setBusy(true);const response=await fetch(`/api/admin/users/${id}`,{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({role:next})});setBusy(false);if(!response.ok){const body=await response.json();alert(body.error);return}router.refresh()}return <select value={role} disabled={busy} onChange={e=>change(e.target.value)}><option value="CUSTOMER">USER</option><option value="USER">USER</option><option value="ADMIN">ADMIN</option></select>}
