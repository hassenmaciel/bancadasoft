"use client";import { FormEvent,useState } from "react";
type Settings={businessName:string;slogan:string;domain:string;supportWhatsapp:string|null;supportEmail:string|null;supportText:string|null;maintenanceEnabled:boolean;maintenanceMessage:string};
export default function SettingsForm({settings}:{settings:Settings}){const[message,setMessage]=useState("");const[saving,setSaving]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);const f=new FormData(e.currentTarget);const payload={businessName:f.get("businessName"),slogan:f.get("slogan"),domain:f.get("domain"),supportWhatsapp:f.get("supportWhatsapp")||null,supportEmail:f.get("supportEmail")||null,supportText:f.get("supportText")||null,maintenanceEnabled:f.get("maintenanceEnabled")==="on",maintenanceMessage:f.get("maintenanceMessage")};const response=await fetch("/api/admin/settings",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const body=await response.json();setSaving(false);setMessage(response.ok?"Configurações salvas.":body.error)}return <form className="admin-form" onSubmit={submit}><div className="form-grid"><label>Nome comercial<input name="businessName" defaultValue={settings.businessName} required/></label><label>Domínio<input name="domain" defaultValue={settings.domain} required/></label></div><label>Slogan<input name="slogan" defaultValue={settings.slogan}/></label><div className="form-grid"><label>WhatsApp de suporte<input name="supportWhatsapp" defaultValue={settings.supportWhatsapp??""}/></label><label>E-mail de suporte<input name="supportEmail" type="email" defaultValue={settings.supportEmail??""}/></label></div><label>Texto de suporte<textarea name="supportText" defaultValue={settings.supportText??""}/></label><label className="check-field"><input name="maintenanceEnabled" type="checkbox" defaultChecked={settings.maintenanceEnabled}/> Ativar modo manutenção no site público</label><label>Mensagem de manutenção<textarea name="maintenanceMessage" defaultValue={settings.maintenanceMessage} required/></label><p className="security-note">Segredos e credenciais permanecem exclusivamente nas variáveis seguras do ambiente.</p>{message&&<p className="form-message">{message}</p>}<div className="form-actions"><button className="admin-primary" disabled={saving}>{saving?"Salvando...":"Salvar configurações"}</button></div></form>}
