"use client";import Image from"next/image";import Link from"next/link";import{useMemo,useState}from"react";import type{AdminProductDTO}from"@/lib/admin-catalog";import{commercialMetrics}from"@/lib/admin-rules";const money=(v:number)=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(v/100);type Option={id:string;name:string};
export default function ProductList({initialProducts,categories,brands}:{initialProducts:AdminProductDTO[];categories:Option[];brands:Option[]}){const[query,setQuery]=useState(""),[status,setStatus]=useState("ALL"),[category,setCategory]=useState("ALL"),[brand,setBrand]=useState("ALL"),[type,setType]=useState("ALL");const products=useMemo(()=>initialProducts.filter(p=>(status==="ALL"||p.status===status)&&(category==="ALL"||p.category.id===category)&&(brand==="ALL"||p.brand?.id===brand)&&(type==="ALL"||p.type===type)&&`${p.name} ${p.slug} ${p.searchTerms}`.toLowerCase().includes(query.toLowerCase())),[initialProducts,query,status,category,brand,type]);return <><header className="admin-heading"><div><small>CATÁLOGO</small><h1>Produtos</h1><p>Classificação, preço, entrega, publicação e descoberta.</p></div><Link className="admin-primary" href="/admin/produtos/novo">Novo produto</Link></header><section className="admin-filters product-filters"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar produto ou termo"/><select value={status} onChange={e=>setStatus(e.target.value)}><option value="ALL">Publicação</option>{["DRAFT","REVIEWED","PUBLISHED","PAUSED","ARCHIVED"].map(x=><option key={x}>{x}</option>)}</select><select value={category} onChange={e=>setCategory(e.target.value)}><option value="ALL">Categorias</option>{categories.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select value={brand} onChange={e=>setBrand(e.target.value)}><option value="ALL">Marcas</option>{brands.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select value={type} onChange={e=>setType(e.target.value)}><option value="ALL">Tipos comerciais</option>{["TOOL","RENTAL","ACTIVATION","LICENSE","CREDIT","IMEI_SN","FILE","REMOTE_SERVICE"].map(x=><option key={x}>{x}</option>)}</select></section><section className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Produto</th><th>Categoria / Marca</th><th>Tipo / Entrega</th><th>Preço</th><th>Custo</th><th>Lucro / Margem</th><th>Publicação</th><th/></tr></thead><tbody>{products.map(product=>{const metrics=commercialMetrics(product.priceCents,product.costCents);return <tr key={product.id}><td><div className="admin-product"><span className="admin-thumb">{product.imageUrl?<Image src={product.imageUrl} alt={product.name} width={42} height={42} unoptimized/>:product.name.slice(0,2).toUpperCase()}</span><div><b>{product.name}</b><small>{product.slug}{product.featured?" · Destaque":""}</small></div></div></td><td>{product.category.name}<small className="cell-subtitle">{product.brand?.name??"Sem marca"}</small></td><td>{product.type}<small className="cell-subtitle">{product.deliveryType}{product.deliveryEstimate?` · ${product.deliveryEstimate}`:""}</small></td><td>{money(product.priceCents)}</td><td>{product.costCents===null?"—":money(product.costCents)}</td><td>{money(metrics.profitCents)}<small className="cell-subtitle">{metrics.marginPercent.toFixed(2)}%</small></td><td><span className={`status-badge status-${product.status.toLowerCase()}`}>{product.status}</span><small className="cell-subtitle">{product.available?"Disponível":"Indisponível"}</small></td><td><Link href={`/admin/produtos/${product.id}`}>Editar</Link></td></tr>})}</tbody></table>{!products.length&&<p className="admin-empty">Nenhum produto corresponde aos filtros.</p>}</section></>}
