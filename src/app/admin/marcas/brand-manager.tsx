"use client";import { FormEvent,useState } from "react";import { useRouter } from "next/navigation";
type Row={id:string;name:string;slug:string;imageUrl:string|null;active:boolean;_count:{products:number}};const slugify=(v:string)=>v.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"");
export default function BrandManager({rows}:{rows:Row[]}){const router=useRouter();const[editing,setEditing]=useState<Row|null>(null);const[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const payload={name:String(f.get("name")),slug:slugify(String(f.get("slug")||f.get("name"))),imageUrl:String(f.get("imageUrl")||"")||null,active:f.get("active")==="on"};const response=await fetch(editing?`/api/admin/brands/${editing.id}`:"/api/admin/brands",{method:editing?"PUT":"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const result=await response.json();setMessage(response.ok?"Marca salva com sucesso.":result.error);if(response.ok){setEditing(null);router.refresh()}}return <div className="category-layout"><form className="admin-form compact" onSubmit={submit}><h2>{editing?"Editar marca":"Nova marca"}</h2><label>Nome<input name="name" defaultValue={editing?.name} key={`n-${editing?.id}`} required/></label><label>Slug<input name="slug" defaultValue={editing?.slug} key={`s-${editing?.id}`}/></label><label>URL do logo<input name="imageUrl" type="url" defaultValue={editing?.imageUrl??""} key={`i-${editing?.id}`}/></label><label className="check-field"><input name="active" type="checkbox" defaultChecked={editing?.active??true} key={`a-${editing?.id}`}/> Ativa</label>{message&&<p className="form-message">{message}</p>}<div className="form-actions"><button className="admin-primary">Salvar</button></div></form><section className="category-list">{rows.map(row=><article key={row.id}><div><b>{row.name}</b><small>{row.slug} · {row._count.products} produtos · {row.active?"Ativa":"Inativa"}</small></div><button onClick={()=>setEditing(row)}>Editar</button></article>)}</section></div>}
