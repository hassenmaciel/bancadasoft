"use client";import Link from "next/link";import{usePathname,useRouter}from"next/navigation";import{useState}from"react";
const links=[["Visão geral","/admin","⌂"],["Produtos","/admin/produtos","◇"],["Categorias","/admin/categorias","▦"],["Marcas","/admin/marcas","◆"],["Pedidos","/admin/pedidos","▤"],["Usuários","/admin/usuarios","♙"],["Integrações","/admin/integracoes","⌁"],["Configurações","/admin/configuracoes","⚙"],["Auditoria","/admin/auditoria","◎"]];
export default function AdminNav(){const path=usePathname();const router=useRouter();const[busy,setBusy]=useState(false);async function logout(){setBusy(true);await fetch("/api/auth/logout",{method:"POST"});router.replace("/login");router.refresh()}return <><nav className="admin-nav" aria-label="Navegação administrativa">{links.map(([label,href,icon])=>{const active=href==="/admin"?path===href:path.startsWith(href);return <Link href={href} key={href} className={active?"active":""}><span aria-hidden>{icon}</span>{label}</Link>})}</nav><button className="admin-logout" onClick={logout} disabled={busy}>{busy?"Saindo...":"Sair com segurança"}</button></>}
