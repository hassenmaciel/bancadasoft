import { prisma } from "@/lib/prisma";
export default async function AuditPage({searchParams}:{searchParams:Promise<{q?:string}>}){const{q}=await searchParams;const logs=await prisma.auditLog.findMany({where:q?{OR:[{action:{contains:q,mode:"insensitive"}},{entityType:{contains:q,mode:"insensitive"}}]}:undefined,include:{actor:{select:{name:true,email:true}}},take:100,orderBy:{createdAt:"desc"}});return <><header className="admin-heading"><div><small>RASTREABILIDADE</small><h1>Logs / Auditoria</h1><p>Registro append-only das ações administrativas relevantes.</p></div></header><form className="admin-filters"><input name="q" defaultValue={q} placeholder="Buscar ação ou entidade"/><button className="admin-primary">Buscar</button></form><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Data</th><th>Administrador</th><th>Ação</th><th>Entidade</th><th>Identificador</th></tr></thead><tbody>{logs.map(log=><tr key={log.id}><td>{log.createdAt.toLocaleString("pt-BR")}</td><td>{log.actor.name}<small className="cell-subtitle">{log.actor.email}</small></td><td>{log.action}</td><td>{log.entityType}</td><td>{log.entityId??"—"}</td></tr>)}</tbody></table></div></>}
