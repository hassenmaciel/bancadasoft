"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export default function ProviderModeControl({initialMode}:{initialMode:"TEST"|"REAL"}){const[mode,setMode]=useState(initialMode);const[pending,setPending]=useState(false);const router=useRouter();async function change(next:"TEST"|"REAL"){if(pending||next===mode)return;setPending(true);const response=await fetch("/api/admin/providers/mode",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({mode:next})});if(response.ok){setMode(next);router.refresh()}setPending(false)}return <div className="provider-mode-control"><b>Modo operacional</b><button disabled={pending||mode==="TEST"} onClick={()=>change("TEST")}>TEST</button><button disabled={pending||mode==="REAL"} onClick={()=>change("REAL")}>REAL</button></div>}
